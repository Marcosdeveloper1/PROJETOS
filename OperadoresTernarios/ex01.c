#include<stdio.h>

int main(){
    int temperatura = 28;
    int resultado;

    temperatura > 30 ? 1: 0; //1 para verdadeiro e 0 para falso
    if(temperatura == 1)
    {
        printf("Está calor!\n");
    }else{
        printf("Está frio!\n");
    }
    return 0;

}